from setuptools import find_packages, setup


setup(
    name="supabase",
    version="0.0.0.post1",
    description="Local stub for supabase to keep installs lightweight in this environment.",
    packages=find_packages(),
)

